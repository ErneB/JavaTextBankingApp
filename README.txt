This is a simple banking app proof of concept that pulls from a JSON "database".
There is no implementation of security, just a simple Java package meant as practice.

This project was originally done for a school project.

It uses the json-simple-1.1.1.jar file I included.